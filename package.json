{
  "name": "@zig-utils/site",
  "version": "0.1.0",
  "private": true,
  "description": "zig-utils.org — the marketing page and the per-library documentation sites",
  "type": "module",
  "scripts": {
    "docs:build": "bun scripts/build-docs.ts",
    "deploy": "bunx --bun @stacksjs/ts-cloud deploy --env production --yes",
    "deploy:diff": "bunx --bun @stacksjs/ts-cloud deploy --env production --dry-run",
    "site:build": "bun scripts/build-site.ts"
  },
  "devDependencies": {
    "@stacksjs/bunpress": "^0.1.20",
    "@stacksjs/stx": "^0.2.99",
    "@stacksjs/ts-cloud": "^0.7.56"
  }
}
